export enum StatusRequest {
  started = 'iniciada',
  inProgress = 'en proceso',
  cancelada = 'cancelada',
}

export enum TypeToast {
  success = 'success',
  error = 'error',
  info = 'info',
}

export enum NavigationRoutes {
  chat = 'Chat',
  chats = 'Chats',
}
